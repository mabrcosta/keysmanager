package com.mabrcosta.keysmanager.core.config.properties

object ServerProperties {
  val CONFIG_KEY = "keysmanager.server"

  val baseURL = "base_url"
  val port = "port"

  val apiBaseURL = "api.base_url"
}
